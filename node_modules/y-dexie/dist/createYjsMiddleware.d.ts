import { DBCore, DBCoreTable } from 'dexie';
import { DbSchema } from 'dexie';
export declare function createYjsMiddleware(dbSchema: DbSchema): (downCore: DBCore) => {
    table(tableName: string): DBCoreTable;
    stack: "dbcore";
    transaction(stores: string[], mode: "readonly" | "readwrite", options?: import("dexie").DbCoreTransactionOptions): import("dexie").DBCoreTransaction;
    MIN_KEY: any;
    MAX_KEY: any;
    schema: import("dexie").DBCoreSchema;
};
